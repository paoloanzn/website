# Credentials for ./stats.sh — copy to .env and fill in. .env is gitignored.
# Account ID:  npx wrangler whoami
CF_ACCOUNT_ID=
# Token with: Account -> Account Analytics -> Read
# https://dash.cloudflare.com/profile/api-tokens
CF_API_TOKEN=
